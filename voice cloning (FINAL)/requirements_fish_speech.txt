# Fish Speech Requirements
# Install Fish Speech first, then these additional dependencies

# Core dependencies (already in your requirements.txt)
numpy>=1.25.2
torch>=2.0.0
torchaudio>=2.0.0
soundfile>=0.12.1
librosa>=0.10.1

# Fish Speech specific (installed via fish-speech package)
# These are included when you run: pip install -e .[cu129]
# Listed here for reference only

# Audio processing
# portaudio19-dev (system package - apt install portaudio19-dev)
# libsox-dev (system package - apt install libsox-dev)
# ffmpeg (system package - apt install ffmpeg)

# Hugging Face for model downloads
huggingface_hub[cli]>=0.19.0

# Optional: For the wrapper
pydub>=0.25.1  # Already in your requirements
simpleaudio>=1.0.4  # For audio preview (optional)

# Development/Testing
pytest>=7.4.0
unittest-xml-reporting>=3.2.0

# Note: Fish Speech itself should be installed separately:
# 1. Clone: git clone https://github.com/fishaudio/fish-speech.git
# 2. Install: cd fish-speech && pip install -e .[cu129]
# 3. Download models: hf download fishaudio/openaudio-s1-mini --local-dir checkpoints/openaudio-s1-mini
